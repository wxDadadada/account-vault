# 验证记录

> 历史归档：保留目录整理前的阶段性验收与部署信息。文中的源码路径、构建哈希、容器状态和本机日志路径对应当时版本。当前检查见 [验证记录](../verification.md)。

日期：2026-09-08。环境：本机 Node.js 22 与 Docker（OrbStack）。

| 检查                                    | 结果                 | 覆盖                                                                         |
| --------------------------------------- | -------------------- | ---------------------------------------------------------------------------- |
| `pnpm format:check`                     | 通过                 | 业务代码、样式、配置及文档格式                                               |
| `pnpm lint`                             | 通过，0 错误、0 警告 | TypeScript、React Hooks 和导出规则                                           |
| `pnpm test`                             | 59 项通过            | 加密、账号域逻辑、多轮对话、真实 SQLite、Fastify API、并发撤销及真实额度限流 |
| `pnpm build`                            | 通过                 | 前端、脚本与测试类型检查，生产构建、Node 后端编译                            |
| 浏览器全量回归及定向复验                | 54 项场景通过        | 桌面与手机视口的聊天、退出确认、未保存保护、锁定、恢复、改密、导入和弹窗回归 |
| `pnpm audit`                            | 0 项已知漏洞         | 当前锁文件的全部依赖，包括前端及开发工具                                     |
| Docker 镜像构建                         | 通过                 | 多阶段构建，运行镜像约 94 MiB                                                |
| `pnpm exec tsx scripts/docker-smoke.ts` | 通过                 | 非 root、只读根目录、静态资源、SPA 深链接、密文写入、重启保留、快照还原      |

依赖审计是截至本次执行的数据库查询结果，不代表后续永远没有新公告。已更新上游模板中的过期间接依赖，包括 [Seroval](https://github.com/advisories/GHSA-mv8w-475r-vwqw) 和 [Vite](https://github.com/advisories/GHSA-fx2h-pf6j-xcff) 相关修复版本。

## 自动化覆盖内容

- 中文账号、密码和备注加密往返，错误主密码与密文篡改被拒绝。
- 登录证明不能直接充当包装密钥，密码轮换与恢复保持账号内容并生成新恢复凭据。
- 便携备份使用原密码或恢复密钥均能恢复。
- 账号创建、修改、删除与恢复历史，重复账号和过期编辑冲突，按账号裁剪历史。
- 主体别名冲突、历史引用保护、导入 ID 映射、重复跳过、保留当前 AI 配置。
- 自然语言常见录入示例、新主体提取、按中国时区解释月份、按真实字段变更筛选。
- 首次初始化唯一性、会话鉴权、跨站写入阻止、密文信封校验及两设备写入冲突。
- SQLite 与 WAL 中没有测试账号的明文密码、备注、邮箱标记。
- SQLite 在线快照独立可读，TOTP 绑定、会话撤销、验证码防重放，恢复与重启登录。
- 远程初始化令牌、HTTPS Cookie、非 HTTPS 远程配置拒绝、备份失败时不误报旧成功结果。
- AI HTTPS 域名和地址范围校验、显式秘密字段阻止、模型输出结构验证。
- 多轮追问平台、账号及主体，支持单字符回答、后续更正、新主体、批量序号与追加草稿；稳定标识保持本地密码与对应草稿的关系。
- 更新目标不明确时追问；保留原账号密码和未修改字段，拒绝过期编辑，批量中任一项失败不会留下部分账号或新主体。
- 查询保留前文，支持主体、收藏、状态和时间增减；空结果可放宽条件，命中数量来自真实本机记录。
- 聊天接口鉴权、跨站阻止、输入/输出结构、对话长度限制、秘密输入拦截，以及在途回复完成时重新检查会话。
- 桌面和手机完成聊天补全、核对、加密保存、再查询；详情返回保留原对话，切换模式保留草稿，密码不进入模型请求。
- 模拟云端多轮录入和查询、中文输入法回车、失败重试与停止回复，停止后不重复发送，跨页面锁定销毁对话。
- 所有手动退出入口均先确认，取消或 Esc 保持当前会话，确认后锁定同浏览器其他页面；保存请求尚未返回时仍能退出，自动空闲锁定不受确认框阻挡。
- 账号、主体和分类编辑有改动时，关闭、取消或 Esc 均先确认；继续编辑完整保留字段与密码，未修改表单可直接关闭。确认框刚打开时快速按 Esc 的边界已连续复验 5 次。
- 聊天录入与查询分别保留未发送输入；关闭或新建会话前保护草稿，未发送的更正会阻止直接保存；“请帮我查一下账号”等表达能够识别查询并继续追问。
- 在 375px 窄屏、横屏、深色主题、125% 字号和减弱动画设置下验证聊天输入、关闭及保存控件；布局断言等待横竖屏切换完成，查询后结果可见。
- WebMCP 工具在模拟注册表中只暂存文字，不保存或读取完整账号库。
- 匿名登录资料不含主密码包装；认证后解锁、密码轮换、TOTP 和原有备份恢复保持可用。
- 在真实 scrypt 执行期间撤销会话、启用 TOTP 或使会话过期，迟到的改密均返回 401，凭据和账号内容保持不变。
- 按明确可信代理还原来源，两个客户端的登录额度独立；不可信转发头、伪造 Cookie 无法绕过限流，超限保留 429 与重试时间，匿名全局额度耗尽不阻断有效会话。
- 导入主体的名称、别名及大小写冲突在合并前被拒绝，失败不修改原数据；合法同名同类型主体继续映射到当前主体，旧歧义数据可打开并手动修复。
- Chromium 的 1280×720 桌面、390×844 手机视口：密码详情与编辑弹窗、关闭和保存按钮定位；截图已人工核对。
- 两页解锁后任一页锁定会移除另一页密码和弹窗；无 BroadcastChannel 的冻结页面恢复后仍锁定；派生密码期间锁定不会发出迟到登录请求。
- 两页共享最近活动时间；真实空闲后同时锁定；改密锁定其他页且保留发起页新会话，并完成新恢复密钥确认。
- 存储锁定代次写入失败时仍通过广播清理另一页；迟到的失效通知不会反向锁定改密成功页，迟到的 TOTP 成功响应不会注销新会话。
- 空白别名在新增与导入时拒绝，在旧数据的录入和查询匹配中忽略，允许用户打开旧数据后修正。IPv4 映射 IPv6 的全网可信范围同样拒绝。

生产构建将 React 和界面依赖拆为稳定公共块，最大 JavaScript 文件由修复前约 614 kB 降为约 269 kB，消除了 500 kB 分块提示。此结果表示单块体积与缓存粒度改善，不代表总下载量等比例降低；各业务弹窗继续按需加载。

## 容器演练

使用独立随机名称的临时容器与数据卷。创建虚构账号并加密保存，生成快照，再写入第二个版本。重启后重新登录并解密，验证第二个版本完整存在。停机后还原前一个快照，再次启动并验证旧版本完整恢复。测试资源已清理，正式数据卷未写入测试账号。

Docker 为随机映射端口的测试容器在重启时可能重新分配宿主机端口。验证脚本会重新读取映射；正式 Compose 使用固定端口 4318。

## 尚未验证的范围

未提供真实模型 API Key，因此没有向外部模型发送实测请求；已验证本地规则、请求结构、返回结构及 AI 转发的目标限制。聊天云端路径使用模拟模型回复覆盖多轮上下文与失败处理，未验证真实模型对复杂中文表达的理解效果；界面提供“测试连接”。

浏览器回归使用本机 Chromium 和手机视口模拟，没有运行 iOS Safari、Android 真机、其他浏览器或真实软键盘测试。WebMCP 只做模拟注册测试，未在真实浏览器代理接口上执行。可信代理路径用真实 Fastify 限流及注入来源验证，未连接公网反向代理/CDN 完整链路。

没有进行第三方独立安全审计、渗透测试、大容量压力测试或服务器跨架构运行验证。当前自动化证明列出的行为，不扩展为未测试功能的保证。

## 本机验证边界

修复验证使用独立临时 SQLite 目录、浏览器端口、Docker 镜像、容器和卷。正式 Compose 数据卷未用于测试。修复版于 2026-09-08 18:18（Asia/Shanghai）更新，聊天增强版于同日 19:03 更新，退出确认与交互完善版于同日 20:29 更新正式服务 `account-vault-app-1`。服务健康，监听 `http://localhost:4318`；容器内 29 个前后端构建文件与最新本机构建逐项一致，HTTP 实际提供的入口已核对为 `/assets/index-D271yNh2.js`，聊天接口要求有效会话。既有 `account-vault_keyfolio_data` 卷继续挂载，账号密文、登录凭据和服务密钥与升级前快照一致，SQLite 完整性检查通过。最新升级前快照保存在卷内 `/app/data/upgrade-backups/interaction-2026-09-08T12-23-32-442Z`，旧镜像保留为 `keyfolio:before-interaction-2026-09-08t12-23-32-442z`。

聊天验证日志位于 `work/chat-enhancement/`：`check.log` 为格式、静态检查、50 项测试及构建结果；`browser-final.log` 包含既有 20 项场景和聊天全量回归；最后的聊天滚动调整由 `browser-chat-final.log` 复验，横竖屏断言修正后由 `browser-layout-final.log` 复验。`docker-smoke.log` 和 `deployment-verification.json` 分别记录最终镜像的独立容器演练与正式服务核对。

退出确认与交互完善的验证日志位于 `work/interaction-polish/`：`check.log` 记录 50 项测试及构建结果，`browser-final.log` 为最终 44 项浏览器全量回归，`browser-discard-repeat.log` 为快速 Esc 边界复验；`docker-build.log`、`docker-smoke.log` 和 `deployment-verification.json` 记录最终镜像构建、独立容器演练及正式服务核对。桌面退出确认、375px 窄屏及深色横屏大字号截图已人工核对。

## 界面重做

按照 `docs/design.md` 重做桌面、手机及深浅主题，使用 shadcn/ui 统一业务控件。首页增加真实账号统计与最近变更，分类／收藏／状态筛选、排序、卡片／列表视图保留。静态检查确认 `src/vault` 没有原生 button、input、select、option、textarea、details、summary、dialog JSX 元素，没有调用浏览器原生 alert、confirm 或 prompt。文件选择入口为自定义组件。

本次补充安全、主体导入与弹窗修复，数据库和便携备份格式仍为版本 1。远程 HTTPS 实例升级前需要补充 `TRUSTED_PROXIES`，本机默认启动方式保持可用。

## 账号库日常管理增强

本轮完成 [日常管理增强](../collection-enhancement.md) 中的全部功能：紧凑首页、平台快捷操作、筛选和保存视图、批量整理、表格导入、别名和模板、回收站、本地账号检查、到期提醒、加密自定义字段和账号级 TOTP。新建账号继承当前主体，显示模式跨页面和重新解锁保留。

- `pnpm check` 通过：格式、ESLint 零错误零警告、59 项测试、前后端类型检查和生产构建。最终配色微调后再次构建通过。
- 浏览器全量 54 项通过；修正手机导入文本框和无障碍跳转链接样式后，新增功能的 10 项桌面／手机场景再次通过。
- 新增域测试覆盖旧数据兼容、敏感字段加密和历史、AI 更新保留扩展字段、批量冲突整体回滚、回收站恢复与清理、秘密排除搜索、到期日期、RFC TOTP 向量、CSV 解析及导入主体映射。
- 人工核对桌面首屏、手机列表、手机导入弹窗、深色主体页及设置顺序；自动化覆盖 375／390px 窄屏、桌面、深色和放大字号。导入期间重建预览会使旧页面引用失效；正式升级后应刷新已打开的页面以载入最新资源。
- 核对浅色／深色正文、辅助文字、按钮、状态提示及侧栏共 18 组配色，均达到 4.5:1。此检查限于列出的颜色令牌组合，不代表已对所有页面完成独立无障碍审计。
- 更新依赖后 `pnpm audit` 查询为 0 项已知漏洞。新增品牌图标只随前端打包，业务弹窗按需加载，最大 JavaScript 文件约 262 kB。

验证日志与配色计算结果保存在 `work/collection-enhancement/`。表格导入使用 CSV／TSV 或 Excel 复制文本；到期提醒在页面打开并解锁时显示。手机验证使用 Chromium 视口模拟，外部模型仍未接入真实密钥。

最终镜像构建和独立 Docker 演练均通过，测试容器与测试卷已清理。日常管理增强版于 2026-09-08 21:56（Asia/Shanghai）更新正式本机服务 `account-vault-app-1`，继续监听 `http://localhost:4318`。容器内 33 个前后端构建文件与最新本机构建逐项一致，HTTP 入口为 `/assets/index-B-V6V-lG.js`，服务健康；账号密文、登录配置和服务密钥与升级前快照一致，数据库完整性检查通过，既有数据卷继续挂载。升级前快照位于 `/app/data/upgrade-backups/collection-2026-09-08T13-55-00-442Z`，旧镜像为 `keyfolio:before-collection-2026-09-08t13-55-00-442z`。

## 界面比例与层级重做

2026-09-08 根据用户视觉反馈重排首页，统一卡片与表格的字体、边框、间距和平台图标，调整主体和设置布局，并把手机新增操作移入底栏。功能逻辑与加密格式保持原有行为；浏览器回收站场景更新为通过“账号工具”进入。

人工核对 1440×960 桌面、1280×720 笔记本和 375／390px 手机视口的浅色／深色首页、卡片、列表、主体、历史和设置页面。笔记本首屏可显示第一排完整账号卡片，手机新增按钮不再覆盖账号操作。自动浏览器回归覆盖原有 54 项场景，包括导入、批量恢复、聊天、锁定、密码轮换、未保存保护和放大字号布局。验证使用独立的临时账号库。日志位于 `work/ui-redesign/`。

最终 `pnpm check` 通过，含格式、ESLint、59 项测试与生产构建。全量浏览器 54 项通过，最后的字号与标签样式调整后 2 项桌面／手机布局复验通过。20 组主题正文配色达到 4.5:1，Docker 独立保存／重启／快照恢复演练通过。

界面新版于 2026-09-08 22:31（Asia/Shanghai）更新本机服务。容器内 33 个构建文件与本机构建一致，HTTP 实际入口 `/assets/index-CBWLLTbO.js` 校验通过。服务健康，账号密文、登录配置、服务密钥与升级前一致，数据库完整性检查通过。快照位于 `/app/data/upgrade-backups/ui-redesign-2026-09-08T14-30-08-315Z`，旧镜像保留为 `keyfolio:before-ui-redesign-2026-09-08t14-30-08-315z`。
